[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = '0.8.26'
evm_version = "cancun"
optimizer_runs = 800
via_ir = false
ffi = true

# Disable metadata hash for deterministic bytecode
bytecode_hash = "none"
cbor_metadata = false